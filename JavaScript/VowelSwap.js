// Write a function reverseVowels(word) that takes a string word as input and reverses only the vowels in the string using the two-pointer technique. The function should return the modified string with the vowels swapped.

// reverseVowels("hello"); // Output: "holle"
// reverseVowels("leetcode"); // Output: "leotcede"
// reverseVowels("aA"); // Output: "Aa"
// reverseVowels("xyz"); // Output: "xyz" (no vowels to swap)

// Input 
const reverseVowels = (word) => {
    const vowels = new Set('aeiouAEIOU');
}
